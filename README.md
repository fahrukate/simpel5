# ~~~~
$ pkg update && pkg updgrade

$ pkg install python

$ pkg install git

$ cd /sdcard

$ git clone https://github.com/fahrukate/simpel5

$ cd simpel5

$ pip install -r requirements.txt

$ python run2.py
